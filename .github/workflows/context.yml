on:
  pull_request:
  pull_request_target:

jobs:
  job-name:
    concurrency:
      group: recheck-${{ github.event.pull_request.number }}
      cancel-in-progress: true
    uses: anthmmatic/public-testbox/.github/workflows/reusewf.yml@v3
